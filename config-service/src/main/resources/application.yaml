spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/Tanush4411/distributed-vortexpay-config-server
      username: Tanush4411
      password: ${GITHUB_PAT}
      default-label: main

server.port: 8888